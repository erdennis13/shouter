<nav class="navbar navbar-default" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Shouter", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <% if signed_in? %>
        <%= form_for(:search, url: search_path, method: :get, html: {class: "navbar-form navbar-right"}) do |form| %>
          <div class="form-group">
            <%= form.text_field :term, placeholder: "search...", class: "form-control" %>
            <%= form.submit "Search", class: "btn btn-default" %>
          </div>
        <% end %>
      <% end %>

      <ul class="nav navbar-nav navbar-right">
        <li class="nav-hov"><%= link_to "Home", root_path %></li>
        <% if signed_in? %>
          <li><%= link_to current_user.username, dashboard_path %></li>
          <li><%= link_to "Sign Out", session_path, method: :delete %></li>  
        <% else %>
          <li><%= link_to "Sign In", new_session_path %></li>
          <li><%= link_to "Sign Up", new_user_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>